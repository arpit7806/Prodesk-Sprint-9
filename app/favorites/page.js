// TODO: build this out — Sprint 8 didn't have a dedicated favorites page in the
// code you shared with me, favorites lived inline via useFavorites + MovieCard hearts.
// This is a placeholder route so the folder exists; wire it up once you decide
// how you want a dedicated /favorites view to look.

export default function FavoritesPage() {
  return (
    <main className="app-shell">
      <h1 className="page-title">Favorites</h1>
      <p>Favorites page — build this out next.</p>
    </main>
  );
}
